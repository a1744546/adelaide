class Main
{
    function void main() {
        var int a, b, c;
        let a = Unix.readInt("");
        let b = Unix.readInt("");
        let c = a + b;
        do Unix.printInt(c) ;
        do Unix.println() ;
        return ;
    }
}